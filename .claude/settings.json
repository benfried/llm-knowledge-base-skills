{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Preparing cloud credentials and vault binding",
            "timeout": 120,
            "command": "DIR=\"${CLAUDE_PROJECT_DIR:-$PWD}\"; if [ -f \"$DIR/scripts/cloud-session-start.sh\" ]; then bash \"$DIR/scripts/cloud-session-start.sh\"; fi; exit 0"
          }
        ]
      }
    ]
  }
}
